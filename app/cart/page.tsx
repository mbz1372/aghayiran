import { products, formatPrice } from "@/lib/data";
export default function Cart(){const items=products.slice(0,3);const total=items.reduce((s,p)=>s+p.price,0);return <main className="container py-5 md:py-10"><h1 className="text-3xl md:text-5xl font-black">سبد خرید</h1><div className="grid lg:grid-cols-3 gap-5 mt-5"><section className="lg:col-span-2 grid gap-3">{items.map(p=><div className="card p-3 flex gap-3" key={p.id}><img src={p.image} className="w-24 h-24 md:w-32 md:h-32 object-cover rounded-2xl"/><div className="flex-1"><b className="text-sm md:text-lg">{p.name}</b><p className="text-forest font-black mt-2 text-sm">{formatPrice(p.price)}</p><p className="text-[11px] text-slate-500 mt-1">{p.shipping}</p><select className="mt-2 rounded-xl border px-3 py-1"><option>۱</option><option>۲</option></select></div></div>)}</section><aside className="card p-5 h-max"><b className="text-xl">خلاصه سفارش</b><div className="flex justify-between mt-5"><span>جمع</span><b>{formatPrice(total)}</b></div><div className="flex justify-between mt-3"><span>ارسال</span><b>رایگان</b></div><a href="/checkout" className="btn btn-primary w-full mt-6">ادامه خرید</a></aside></div></main>}
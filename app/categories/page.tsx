import Link from "next/link";import { categories } from "@/lib/data";
export default function Categories(){return <main className="container py-12"><h1 className="text-4xl font-black">دسته‌بندی محصولات</h1><div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-5 mt-8">{categories.map(c=><Link className="card p-8 hover:-translate-y-1 transition" href={`/shop?cat=${c.slug}`} key={c.slug}><div className="text-5xl">{c.icon}</div><h2 className="text-2xl font-black mt-5">{c.title}</h2><p className="text-slate-500 mt-2">{c.count} کالا آماده فروش</p></Link>)}</div></main>}
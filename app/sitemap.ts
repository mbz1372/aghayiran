import type {MetadataRoute} from "next";import {products} from "@/lib/data";export default function sitemap():MetadataRoute.Sitemap{const base="https://aghayeiran.vercel.app";return [{url:base,lastModified:new Date(),priority:1},{url:`${base}/shop`,lastModified:new Date(),priority:.9},{url:`${base}/compare`,lastModified:new Date(),priority:.7},{url:`${base}/blog`,lastModified:new Date(),priority:.7},...products.map(p=>({url:`${base}/product/${p.id}`,lastModified:new Date(),priority:.8}))]}
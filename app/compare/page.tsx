import { products, formatPrice } from "@/lib/data";
export default function Compare(){const ps=products.slice(0,3);return <main className="container py-12"><h1 className="text-4xl font-black">مقایسه محصولات</h1><div className="card p-6 mt-8 overflow-x-auto"><table className="w-full min-w-[760px] text-sm"><thead><tr>{ps.map(p=><th key={p.id} className="p-4"><img src={p.image} className="h-36 w-full object-cover rounded-2xl"/><b className="block mt-3">{p.name}</b></th>)}</tr></thead><tbody>{["price","rating","warranty","shipping","size"].map(row=><tr className="border-t" key={row}>{ps.map(p=><td className="p-4 text-center" key={p.id+row}>{row==="price"?formatPrice(p.price):row==="rating"?p.rating:row==="warranty"?p.warranty:row==="shipping"?p.shipping:p.size}</td>)}</tr>)}</tbody></table></div></main>}
"use client";import {products,formatPrice} from "@/lib/data";import {Check,X} from "lucide-react";export default function Compare(){const ps=products.slice(0,3);return <main className="container section"><div className="section-head"><div><span className="eyebrow text-forest">COMPARE LAB</span><h2>مقایسه بی‌طرفانه</h2><p className="muted">سه مدل را در عملکرد، وزن، قیمت و کاربرد کنار هم ببین.</p></div></div><div className="shell table-wrap"><table className="table"><thead><tr><th>ویژگی</th>{ps.map(p=><th key={p.id}><img src={p.image} className="w-52 h-36 object-cover rounded-2xl mb-3"/><div className="font-black">{p.name}</div></th>)}</tr></thead><tbody><tr><td>قیمت</td>{ps.map(p=><td key={p.id} className="price">{formatPrice(p.price)}</td>)}</tr><tr><td>موجودی</td>{ps.map(p=><td key={p.id}>{p.stock.toLocaleString('fa-IR')}</td>)}</tr><tr><td>امتیاز</td>{ps.map(p=><td key={p.id}>{p.rating}</td>)}</tr><tr><td>کاربری</td>{ps.map(p=><td key={p.id}>{p.category}</td>)}</tr>{[0,1,2,3].map(i=><tr key={i}><td>ویژگی {i+1}</td>{ps.map(p=><td key={p.id}><span className="flex gap-2"><Check size={17} className="text-forest"/>{p.specs[i]||'-'}</span></td>)}</tr>)}</tbody></table></div></main>}
import ProductCard from "@/components/ProductCard";import { products } from "@/lib/data";import { Search } from "lucide-react";export default function SearchPage(){return <main className="container py-5"><div className="card p-3 flex gap-2 items-center"><Search size={18}/><input autoFocus className="outline-none w-full" placeholder="چی می‌خوای بخری؟"/></div><h1 className="text-2xl font-black mt-6">پیشنهادهای سریع</h1><div className="grid grid-cols-2 gap-3 mt-4">{products.slice(0,6).map(p=><ProductCard key={p.id} p={p} compact/>)}</div></main>}
"use client";
import Link from "next/link";import {createContext,useContext,useState} from "react";import {Scale,X} from "lucide-react";import {products} from "@/lib/data";
const C=createContext<any>(null);export function CompareProvider({children}:{children:React.ReactNode}){const [ids,setIds]=useState<string[]>([]);const toggle=(id:string)=>setIds(x=>x.includes(id)?x.filter(i=>i!==id):x.length<3?[...x,id]:x);return <C.Provider value={{ids,toggle,has:(id:string)=>ids.includes(id)}}>{children}{ids.length>0&&<div className="compare-bar"><Scale size={18}/><span>{ids.length.toLocaleString('fa-IR')} محصول برای مقایسه</span><Link href="/compare" className="btnx btn-acid !min-h-10 !py-2">مقایسه</Link><button onClick={()=>setIds([])}><X size={18}/></button></div>}</C.Provider>}export const useCompare=()=>useContext(C);